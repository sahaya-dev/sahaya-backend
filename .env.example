PORT=4000
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/sahaya
JWT_SECRET=your-super-secret-jwt-key-change-in-production
OTP_EXPIRY_MINUTES=10

# Optional: force-enable dev DB insert API when NODE_ENV is not development
# ENABLE_DEV_DB_TOOLS=true

# Optional: require this header for all /api/dev/* requests: X-Dev-Db-Key: <value>
# DEV_DB_KEY=change-me-in-dev-only
